bundle:
  name: model-guru

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    model-guru-app:
      name: "model-guru"
      description: "model-guru created with apx"
      source_code_path: ./.build
      user_api_scopes:
        - sql.statement-execution
        - sql.warehouses:read
        - catalog.catalogs:read
        - catalog.schemas:read
        - catalog.tables:read
        - serving.serving-endpoints
      permissions:
        - user_name: jonathan.whiteley@databricks.com
          level: CAN_MANAGE

targets:
  dev:
    mode: development
    default: true
